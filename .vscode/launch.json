{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Kite VS Code Extension",
      "type": "extensionHost",
      "request": "launch",
      "runtimeExecutable": "${execPath}",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}/vscode-kite",
        "${workspaceFolder}/examples"
      ],
      "outFiles": ["${workspaceFolder}/vscode-kite/out/**/*.js"],
      "preLaunchTask": "kite-extension: debug prep"
    }
  ]
}
