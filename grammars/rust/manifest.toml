language = "rust"
version = "0.24.0"
wasm_file = "tree-sitter-rust.wasm"
extensions = [".rs"]
display_name = "Rust"

[queries]
symbol_exists = "queries/symbol_exists.scm"
symbol_arity = "queries/symbol_arity.scm"

[arity]
parameter_kinds = ["parameter", "variadic_parameter"]


[queries.boundary_references]
source = """
[
  (use_declaration)
  (type_identifier)
  (scoped_identifier)
  (call_expression)
] @reference
"""
